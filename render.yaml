services:
  # Spring Boot Backend
  - type: web
    name: fooddash-api
    env: docker
    plan: free
    dockerContext: .
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        sync: false   # Set this manually in Render dashboard
      - key: REDIS_URL
        sync: false   # Set this after adding Redis service below
      - key: ELASTICSEARCH_URL
        sync: false   # Set this after setting up Bonsai/Elastic
      - key: FRONTEND_URL
        sync: false   # Set to your Vercel URL e.g. https://fooddash.vercel.app
      - key: SPRING_PROFILES_ACTIVE
        value: prod

  # Redis (optional — uncomment after upgrading plan or using free Redis add-on)
  # - type: redis
  #   name: fooddash-redis
  #   plan: free
  #   maxmemoryPolicy: allkeys-lru
